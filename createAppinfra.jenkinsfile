pipeline{
agent any
stages{
  stage('Create stack'){
    steps{
       sh "aws cloudformation create-stack --stack-name ${STACK_NAME} --template-body file://${WORKSPACE}/ec2-alb-app.yaml --region 'ap-south-1' --parameters ParameterKey=keyName,ParameterValue=${KEY_NAME}"
    }
  }
}
}
